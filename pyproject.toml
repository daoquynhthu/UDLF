[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "udlf"
version = "0.0.0"
description = "Unified Diffusive Latent-Field language model research workspace"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
